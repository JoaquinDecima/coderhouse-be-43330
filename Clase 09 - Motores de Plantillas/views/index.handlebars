<h1>Hola {{user.name}}</h1>

<p>
	Tu tienes
	{{user.edad}}
	años, vives en
	{{user.city}}
	tu email es
	{{user.mail}}
	y puedo llamarte al
	{{user.tel}}
</p>

{{#if isAdmin}}
	<ul>
		{{#each foods}}
			<li>{{this.name}} - {{this.price}}</li>
		{{/each}}
	</ul>
{{else}}
	<h2>No tenes permisos, ¿que miras? aca vos no comes...</h2>
{{/if}}

<h1>Hay {{cant}} de Usuarios registrados</h1>